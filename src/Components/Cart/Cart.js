import React from 'react';
import './Cart.css';
import PropTypes from 'prop-types';

const Cart = () => {
  return <section className='cart'>Cart</section>;
};

export default Cart;

Cart.propTypes = {
  // logoutUser: PropTypes.func
};
